# BenchmarkOS Chatbot - Environment Variables Example
# Copy this file to .env and fill in your actual values
# The .env file is gitignored and will not be committed
#
# Quick setup:
#   Windows: Copy-Item .env.example .env
#   macOS/Linux: cp .env.example .env
# Then edit .env and uncomment/fill in the values you need

# ============================================
# REQUIRED - Core Configuration
# ============================================

# LLM Provider (required)
# Options: "openai" or "local"
# - "openai": Use OpenAI API (requires OPENAI_API_KEY)
# - "local": Use local echo LLM (for testing, no API key needed)
LLM_PROVIDER=openai

# OpenAI API Key (required if LLM_PROVIDER=openai)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenAI Model (optional)
# Default: gpt-4o-mini
# OPENAI_MODEL=gpt-4o-mini

# SEC API User Agent (required for SEC API requests)
# Format: YourName/1.0 (your.email@example.com)
# This identifies your application to the SEC
SEC_API_USER_AGENT=BenchmarkOSBot/1.0 (support@benchmarkos.com)

# ============================================
# OPTIONAL - FRED API (Macroeconomic Data)
# ============================================

# FRED API Key (optional, for live macroeconomic data)
# Get your free key from: https://fred.stlouisfed.org/docs/api/api_key.html
# If not provided, system uses default hardcoded macro values
# FRED_API_KEY=your-fred-api-key-here

# ============================================
# OPTIONAL - Database Configuration
# ============================================

# Database Type (optional)
# Options: "sqlite" (default) or "postgresql"
# DATABASE_TYPE=sqlite

# SQLite Database Path (optional, only if using SQLite)
# Default: ./data/sqlite/benchmarkos_chatbot.sqlite3
# DATABASE_PATH=./data/sqlite/benchmarkos_chatbot.sqlite3

# PostgreSQL Connection (optional, only if using PostgreSQL)
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=secdb
# PGUSER=postgres
# PGPASSWORD=your_secure_password_here

# ============================================
# OPTIONAL - Advanced Configuration
# ============================================

# Data Cache Directory (optional)
# Default: ./cache
# DATA_CACHE_DIR=./cache

# Yahoo Finance Batch Size (optional)
# Default: 50
# YAHOO_QUOTE_BATCH_SIZE=50

# HTTP Request Timeout (optional, in seconds)
# Default: 30
# HTTP_REQUEST_TIMEOUT=30

# Ingestion Max Workers (optional)
# Default: 8
# INGESTION_MAX_WORKERS=8
